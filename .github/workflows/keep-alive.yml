name: Keep Render Alive
on:
  schedule:
    - cron: "*/14 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Backend
        run: curl -s https://onu-cards.onrender.com/health || echo "Ping failed"
